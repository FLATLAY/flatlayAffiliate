const customerSubscriptionDeleted = ({ data }) => {
	try {
		/**
		 * Update user SQL:
		 *
		 * subscription {
		 *   id: ''
		 *   plan: ''
		 *   current_period_end: ''
		 *   status: 'none'
		 * }
		 *
		 */
	} catch (exception) {
		throw (exception);
	}
};

export default customerSubscriptionDeleted;
